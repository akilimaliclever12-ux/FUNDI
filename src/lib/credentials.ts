import type { CredentialType } from "@/types/database.types";

export const CREDENTIAL_LABEL: Record<CredentialType, string> = {
  certification: "Certification",
  diploma: "Diplôme",
  attestation: "Attestation de service rendu",
};
